---
phase: 01-complete-landing-page
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - index.html
autonomous: true
requirements:
  - DESIGN-05
  - DESIGN-06
  - CONTENT-01
  - CONTENT-02
  - CONTENT-06
  - LEAD-01
  - TECH-02

must_haves:
  truths:
    - "Visitor sees a full-viewport hero with dramatic headline, subheadline, and CTA button"
    - "Visitor can navigate via sticky nav on desktop and hamburger menu on mobile"
    - "Visitor reads problem agitation statements that challenge their manufacturing performance"
    - "Hero CTA opens email client addressed to f.gloebl@g-und-p.de"
  artifacts:
    - path: "index.html"
      provides: "HTML page with head, nav, hero, and problem sections"
      contains: "Dramatische Leistungssteigerung"
      min_lines: 150
  key_links:
    - from: "index.html nav links"
      to: "section IDs (#pillars, #scb, #cta)"
      via: "anchor href"
      pattern: "href=\"#"
    - from: "index.html hero CTA"
      to: "f.gloebl@g-und-p.de"
      via: "mailto link"
      pattern: "mailto:f.gloebl@g-und-p.de"
    - from: "index.html <style>"
      to: "css/input.css theme tokens"
      via: "Tailwind CDN @theme block"
      pattern: "--color-gp-"
---

<objective>
Create the index.html with complete HTML head, navigation bar, hero section, and problem agitation section. This establishes the page skeleton that all subsequent plans build upon.

Purpose: The top half of the page -- from first load to the problem statement -- must immediately communicate who G&P is and create emotional tension.
Output: index.html with working nav, hero, and problem section, viewable in browser with Tailwind CDN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-complete-landing-page/01-RESEARCH.md
@.planning/phases/01-complete-landing-page/01-UI-SPEC.md
@.planning/phases/01-complete-landing-page/01-01-SUMMARY.md
@css/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with head, Tailwind theme, nav bar, and skip link</name>
  <files>index.html</files>
  <read_first>
    .planning/phases/01-complete-landing-page/01-UI-SPEC.md
    .planning/phases/01-complete-landing-page/01-RESEARCH.md
    css/input.css
  </read_first>
  <action>
Create `index.html` with:

**DOCTYPE and html tag:**
- `<!DOCTYPE html>` and `<html lang="de">` (per DESIGN-06 for German hyphenation)

**Head section containing:**
- `<meta charset="UTF-8">`
- `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- `<title>G&amp;P - Dramatische Leistungssteigerung fuer produzierende Unternehmen</title>`
- `<meta name="description" content="G&amp;P - Dramatische Leistungssteigerung fuer produzierende Unternehmen. ERP &amp; KI, Lean, Organisation. Beratung fuer Geschaeftsfuehrer.">`
- Font preconnect links:
  ```html
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400&family=Space+Grotesk:wght@700&display=swap" rel="stylesheet">
  ```
- Tailwind CDN script (development):
  ```html
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  ```
- Inline `<style type="text/tailwindcss">` block containing the EXACT same @import and @theme block as css/input.css (so CDN mode picks up the theme). Copy the full contents of css/input.css into this style tag.
- GSAP CDN scripts (deferred):
  ```html
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js" defer></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/ScrollTrigger.min.js" defer></script>
  ```
- OG meta tags (placeholder values, finalized in plan 05):
  ```html
  <meta property="og:title" content="G&amp;P - Hoechstleistung fuer Ihre Produktion">
  <meta property="og:description" content="Dramatische Leistungssteigerung fuer produzierende Unternehmen. ERP &amp; KI, Lean, Organisation.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="assets/images/og-image.jpg">
  ```

**Body starts with skip link (accessibility):**
```html
<a href="#hero" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:bg-gp-red focus:text-gp-white focus:px-4 focus:py-2 focus:rounded">
  Zum Inhalt springen
</a>
```

**Navigation bar:**
```html
<nav class="fixed top-0 inset-x-0 z-40 transition-colors duration-300" aria-label="Hauptnavigation">
  <div class="mx-auto max-w-7xl px-6 lg:px-8 flex items-center justify-between h-20">
    <!-- Logo/brand (text-based per research: no logo file exists) -->
    <a href="#" class="font-heading text-2xl font-bold text-gp-white tracking-tight">G&amp;P</a>

    <!-- Desktop nav links (hidden on mobile) -->
    <div class="hidden lg:flex items-center gap-8">
      <a href="#pillars" class="font-body text-base text-gp-white/70 hover:text-gp-white transition-opacity duration-150">Schwerpunkte</a>
      <a href="#scb" class="font-body text-base text-gp-white/70 hover:text-gp-white transition-opacity duration-150">Sch(B)rechstunde</a>
      <a href="#cta" class="font-body text-base text-gp-white/70 hover:text-gp-white transition-opacity duration-150">Kontakt</a>
      <a href="mailto:f.gloebl@g-und-p.de" class="inline-block bg-gp-red hover:bg-gp-red-hover text-gp-white font-bold text-base px-6 py-2.5 rounded transition-colors duration-200">
        Kontakt
      </a>
    </div>

    <!-- Mobile hamburger button -->
    <button id="menu-btn" class="lg:hidden text-gp-white" aria-expanded="false" aria-controls="menu-overlay">
      <span class="sr-only">Menue oeffnen</span>
      <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
      </svg>
    </button>
  </div>

  <!-- Mobile menu overlay -->
  <div id="menu-overlay" class="hidden fixed inset-0 z-50 bg-gp-black/98 backdrop-blur-sm flex flex-col items-center justify-center gap-8" aria-hidden="true">
    <button id="menu-close" class="absolute top-6 right-6 text-gp-white">
      <span class="sr-only">Menue schliessen</span>
      <svg class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
    <a href="#pillars" class="font-heading text-2xl text-gp-white">Schwerpunkte</a>
    <a href="#scb" class="font-heading text-2xl text-gp-white">Sch(B)rechstunde</a>
    <a href="#cta" class="font-heading text-2xl text-gp-white">Kontakt</a>
    <a href="mailto:f.gloebl@g-und-p.de" class="mt-4 inline-block bg-gp-red hover:bg-gp-red-hover text-gp-white font-bold text-lg px-8 py-3 rounded transition-colors duration-200">
      Jetzt Kontakt aufnehmen
    </a>
  </div>
</nav>
```

Per CONTENT-06: Nav works on mobile (hamburger) and desktop (sticky/fixed).
Per DESIGN-05: Mobile-first responsive with lg: breakpoint for desktop nav.

Focus visible states: Add `focus-visible:ring-2 focus-visible:ring-gp-red focus-visible:ring-offset-2 focus-visible:ring-offset-gp-black` to all interactive elements (links and buttons) in the nav.
  </action>
  <verify>
    <automated>test -f index.html && grep -q 'lang="de"' index.html && grep -q "menu-btn" index.html && grep -q "menu-overlay" index.html && grep -q "Schwerpunkte" index.html && grep -q "sr-only" index.html && grep -q "Zum Inhalt springen" index.html && grep -q "@tailwindcss/browser@4" index.html && grep -q "gsap@3.14.2" index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<html lang="de">`
    - index.html contains `<meta charset="UTF-8">`
    - index.html contains `<meta name="viewport"`
    - index.html contains Google Fonts preconnect links for fonts.googleapis.com and fonts.gstatic.com
    - index.html contains `family=Space+Grotesk:wght@700` and `family=Inter:wght@400`
    - index.html contains `@tailwindcss/browser@4` CDN script
    - index.html contains `<style type="text/tailwindcss">` with full @theme block matching css/input.css
    - index.html contains `gsap@3.14.2` script tags with `defer`
    - index.html contains skip link with text "Zum Inhalt springen"
    - index.html nav contains `id="menu-btn"` with `aria-expanded="false"`
    - index.html nav contains `id="menu-overlay"` with `aria-hidden="true"`
    - index.html nav contains `id="menu-close"` button
    - index.html nav contains `<span class="sr-only">Menue oeffnen</span>`
    - index.html nav contains `<span class="sr-only">Menue schliessen</span>`
    - index.html nav links include href="#pillars", href="#scb", href="#cta"
    - index.html contains `og:title` and `og:description` meta tags
    - All interactive elements in nav have focus-visible ring classes
  </acceptance_criteria>
  <done>index.html exists with valid HTML5 doctype, complete head with fonts/CDN/OG, accessible skip link, and responsive nav with hamburger mobile menu and desktop horizontal links.</done>
</task>

<task type="auto">
  <name>Task 2: Add hero section and problem agitation section to index.html</name>
  <files>index.html</files>
  <read_first>
    index.html
    .planning/phases/01-complete-landing-page/01-UI-SPEC.md
    .planning/phases/01-complete-landing-page/01-RESEARCH.md
  </read_first>
  <action>
Add the hero section and problem section inside `<main>` after the nav, before the closing `</body>`.

**Hero section** (per CONTENT-01, LEAD-01):
```html
<main>
  <section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Background image with dark overlay -->
    <div class="absolute inset-0">
      <picture>
        <source srcset="assets/images/img_0350.avif" type="image/avif">
        <source srcset="assets/images/img_0350.webp" type="image/webp">
        <img src="assets/images/img_0350.jpg" alt="Produktionshalle mit modernen Maschinen"
             class="w-full h-full object-cover" width="1920" height="1080" fetchpriority="high" decoding="async">
      </picture>
      <div class="absolute inset-0 bg-gradient-to-b from-gp-black/70 via-gp-black/50 to-gp-black"></div>
    </div>

    <!-- Hero content -->
    <div class="relative z-10 mx-auto max-w-7xl px-6 lg:px-8 text-center">
      <h1 class="font-heading text-4xl lg:text-7xl font-bold leading-tight tracking-tight text-gp-white">
        Dramatische <span class="text-gp-red">Leistungssteigerung</span> fuer produzierende Unternehmen
      </h1>
      <p class="subheadline mt-6 font-body text-lg md:text-xl leading-relaxed text-gp-light max-w-3xl mx-auto">
        Kille die Hoechstleistungskiller in Deiner Firma
      </p>
      <div class="mt-10">
        <a href="mailto:f.gloebl@g-und-p.de"
           class="cta-btn inline-block bg-gp-red hover:bg-gp-red-hover text-gp-white font-bold text-lg md:text-xl px-8 py-4 rounded transition-all duration-200 hover:scale-[1.02] focus-visible:ring-2 focus-visible:ring-gp-red focus-visible:ring-offset-2 focus-visible:ring-offset-gp-black">
          Jetzt Kontakt aufnehmen
        </a>
      </div>
    </div>
  </section>
```

The hero image uses `img_0350` (the first converted photo). NO `loading="lazy"` on hero image -- it is the LCP element with `fetchpriority="high"`.
The word "Leistungssteigerung" is wrapped in `<span class="text-gp-red">` per UI-SPEC accent color reserved for hero headline emphasis.

**Problem agitation section** (per CONTENT-02):
```html
  <section id="problem" class="bg-gp-charcoal py-12 md:py-16 lg:py-24">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
      <h2 class="font-heading text-3xl lg:text-5xl font-bold leading-tight text-gp-white mb-12">
        Warum die meisten Fabriken unter ihrem Potenzial bleiben
      </h2>
      <div class="space-y-8 max-w-prose">
        <p class="problem-item font-body text-lg md:text-xl leading-relaxed text-gp-light">
          Deine ERP-Einfuehrung hat Millionen gekostet -- und trotzdem plant Dein Team in Excel. KI-Projekte versanden, weil niemand die Prozesse versteht, die sie optimieren sollen.
        </p>
        <p class="problem-item font-body text-lg md:text-xl leading-relaxed text-gp-light">
          Fuehrungskraefte reden ueber Veraenderung, aber die Organisation wehrt sich mit jeder Faser. Hoechstleistung bleibt ein Vorsatz, kein Zustand.
        </p>
        <p class="problem-item font-body text-lg md:text-xl leading-relaxed text-gp-light">
          Lean war mal ein Projekt. Jetzt stauben die Boards ein. Wertschoepfung passiert trotz des Systems, nicht wegen ihm.
        </p>
      </div>
      <!-- Rhythm CTA per LEAD-03 -->
      <div class="mt-12">
        <a href="mailto:f.gloebl@g-und-p.de"
           class="inline-block font-heading text-lg text-gp-red hover:text-gp-red-hover transition-colors duration-200 font-bold focus-visible:ring-2 focus-visible:ring-gp-red focus-visible:ring-offset-2 focus-visible:ring-offset-gp-charcoal">
          Sprich mit Frank &rarr;
        </a>
      </div>
    </div>
  </section>
```

The problem section uses `bg-gp-charcoal` per UI-SPEC section background alternation pattern.
Three problem statements as separate `<p>` tags with class `problem-item` for GSAP stagger animation targeting.
"Sprich mit Frank" rhythm CTA per LEAD-03 (multiple CTA touchpoints throughout page).

Ensure there is a closing `</main>` after the problem section (subsequent sections will be added in plan 03 before this closing tag).

Per TECH-02: OG meta tags are already in the head from Task 1.
  </action>
  <verify>
    <automated>grep -q 'id="hero"' index.html && grep -q 'min-h-screen' index.html && grep -q 'Dramatische' index.html && grep -q 'text-gp-red.*Leistungssteigerung' index.html && grep -q 'mailto:f.gloebl@g-und-p.de' index.html && grep -q 'id="problem"' index.html && grep -q 'bg-gp-charcoal' index.html && grep -q 'problem-item' index.html && grep -q 'Sprich mit Frank' index.html && grep -q 'fetchpriority="high"' index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<section id="hero"` with `min-h-screen`
    - index.html contains `<h1>` with "Dramatische" and "Leistungssteigerung"
    - index.html h1 contains `<span class="text-gp-red">Leistungssteigerung</span>`
    - index.html hero contains `<picture>` element with AVIF, WebP, and JPEG sources
    - index.html hero img has `fetchpriority="high"` and NO `loading="lazy"`
    - index.html hero img has explicit `width` and `height` attributes
    - index.html hero contains gradient overlay with `from-gp-black/70 via-gp-black/50 to-gp-black`
    - index.html hero CTA links to `mailto:f.gloebl@g-und-p.de`
    - index.html hero CTA text is "Jetzt Kontakt aufnehmen"
    - index.html contains `<section id="problem"` with `bg-gp-charcoal`
    - index.html problem section has `<h2>` with "Warum die meisten Fabriken"
    - index.html problem section contains 3 elements with class `problem-item`
    - index.html problem section contains "Sprich mit Frank" rhythm CTA with mailto link
    - index.html hero and problem sections are inside `<main>` element
    - All CTA links have focus-visible ring classes
  </acceptance_criteria>
  <done>index.html has hero section (full viewport, background image with overlay, dramatic headline with red accent, subheadline, primary CTA) and problem section (3 agitation statements, rhythm CTA). Page is viewable in browser with Tailwind CDN.</done>
</task>

</tasks>

<verification>
- Open `index.html` in browser: page shows dark background, nav bar, full-screen hero with headline and CTA, problem section below
- Nav hamburger button visible on mobile viewport, horizontal links on desktop
- Hero image loads (not lazy), gradient overlay darkens the image
- `mailto:f.gloebl@g-und-p.de` link works in hero CTA
- Text is readable (white on dark backgrounds)
- German text renders without overflow on mobile viewport (375px width)
</verification>

<success_criteria>
A visitor can open index.html in a browser and see: sticky nav with G&P brand, full-viewport hero with dramatic headline and working email CTA, and a problem section with 3 provocative statements. Page uses Tailwind CDN for development.
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-landing-page/01-02-SUMMARY.md`
</output>
