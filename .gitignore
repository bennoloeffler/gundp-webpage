# Tailwind CSS standalone CLI binary (downloaded by build.sh)
tailwindcss-macos-arm64

# macOS
.DS_Store

# Reference screenshots (not for website)
reference-*.png

# Original HEIC source photos (not for web)
bilder-landshut-asis/
*.claude-backup
